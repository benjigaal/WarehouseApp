{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "ConnectionStrings": { "MySql": "server=localhost;database=warehouse;user=root;password=;sslmode=none;" },
  "AuthSettings": {
    "JwtOptions": {
      "Secret": "In the end, entropy rules us all", 
      "Issuer": "auth-api",
      "Audience": "auth-client"
    }
  } 
  
}
